function y = maxn(x)
% FUNCTION y = maxn(x)
%   Computes the max of all values in x.  Don't know why matlab makes this
%   so hard.
%      y = max(x(:));
%
% PARAMETERS
%   x  : Any matrix
%
% RETURNS
%   y  : The max, a scalar
%

  y = max(x(:));