function y = sumn(x)
% FUNCTION y = sumn(x)
%   Computes the sum of all values in x.  Don't know why matlab makes this
%   so hard.
%      y = sum(x(:));
%
% PARAMETERS
%   x  : Any matrix
%
% RETURNS
%   y  : The sum, a scalar
%

  y = sum(x(:));

end